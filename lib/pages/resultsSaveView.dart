
import 'package:flutter/widgets.dart';

class SaveView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(child: new Text("Show results as charts"),);
  }

}