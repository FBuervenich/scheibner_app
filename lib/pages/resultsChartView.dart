import 'package:flutter/widgets.dart';

class ChartView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Write comments and save simulation data"),
    );
  }
}
