import 'package:flutter/material.dart';

class ergebnissePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        //TODO add ergebnissePage
        child: Text('ergebnissePage'),
      ),
    );
  }
}