import 'package:flutter/widgets.dart';

class TableView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Show result data as table"),
    );
  }
}
